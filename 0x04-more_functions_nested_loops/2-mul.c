#include "main.h"

/**
 * mul - multiplies tow integers
 * @x: the first number to multiply
 * @y: the second number to multiply
 * Return: the result of multiplicatoin between x and y
 */

int  mul(int x, int y)
{
	return (x * y);
}
